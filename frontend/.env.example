# Backend API base URL (no trailing slash). Must match your uvicorn port.
VITE_API_URL=http://127.0.0.1:8001

# Feature toggle: set to "false" to hide the AI Search panel (upload-only UI).
# Any value other than "false" shows search.
VITE_ENABLE_AI_SEARCH_UI=true
